# Core data pipeline
vaderSentiment==3.3.2
langdetect==1.0.9
scipy==1.13.1

# HuggingFace validation pass (METHODOLOGY.md section 2)
# torch is CPU-only; on first run pipeline/classify.py downloads the
# cardiffnlp/twitter-xlm-roberta-base-sentiment model (~1.1GB) from
# huggingface.co - requires internet access once, then it's cached locally.
torch==2.8.0
transformers==4.57.6
sentencepiece==0.2.2
protobuf==6.33.6

# Backend
fastapi==0.128.8
uvicorn[standard]==0.39.0
jinja2==3.1.6

# Testing
pytest==8.4.2
httpx==0.28.1

# PDF report export (step 9 / PROJECT_INSTRUCTIONS.md section 5 "Reporting")
reportlab==5.0.0

# Optional: only needed for the real collection path (pipeline/collect.py)
# once real API keys are configured in .env. Not required to run the app
# on the bundled sample data.
python-dotenv==1.2.1
google-api-python-client==2.198.0
